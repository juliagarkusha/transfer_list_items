// External deps
import React from 'react';
import ReactDOM from 'react-dom/client';

//Internal deps
import App from "./components/TransferList/TransferList";

// Local deps
import './index.scss';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
